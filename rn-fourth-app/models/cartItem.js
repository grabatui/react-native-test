export default class CartItem {
    constructor(productId, quantity, price, title) {
        this.productId = productId;
        this.quantity = quantity;
        this.price = price;
        this.title = title;
    }
}
